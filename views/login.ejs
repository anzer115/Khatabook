<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" href="/public/css/style.css">
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <main class="w-full h-screen bg-zinc-100 font-['Helvetica']">
          <nav class="flex px-10 justify-between py-5">
            <h3 class="text-2xl tracking-tight">Khaatabook</h3>
           </nav>
        <div class="flex-grow flex justify-center items-center">
            <div class="w-60 flex flex-col gap-3 my-40">
                <h2 class="text-4xl  font-['Helvetica'] tracking-tight">Login your account</h2>
                <form action="/login" method="post" class="flex flex-col space-y-4">
                    <input type="email" placeholder="Email" name="email" class="border border-gray-300 p-2 rounded-md focus:ring-indigo-500 focus:border-indigo-500">
                    <input type="password" placeholder="Password" name="password" class="border border-gray-300 p-2 rounded-md focus:ring-indigo-500 focus:border-indigo-500">
                    <div class="flex justify-start mt-4">
                        <input type="submit" class="bg-blue-600 px-5 py-2 text-sm tracking-tight text-white rounded-md hover:bg-blue-800 cursor-pointer" value="Login" >
                        <div class="text-sm tracking-tight mx-3 px-5 bg-green-600 text-white rounded-md hover:bg-green-800 cursor-pointer ">
                            <a href="/create">Create a new account</a>
                        </div>
                       </div>
                       <% if(message) { %>
                        <div class="text-sm tracking-tight text-green-600">
                          <%= message %>
                        </div>
                      <% } %>
                      <% if(failuremessage) { %>
                        <div class="text-sm tracking-tight text-red-600">
                          <%= failuremessage %>
                        </div>
                      <% } %>
                      <% if(failureReg) { %>
                        <div class="text-sm tracking-tight text-red-600">
                          <%= failureReg %>
                        </div>
                      <% } %>
                   
                  
                </form>
            </div>
        </div>
    </main>
    
</body>
</html>

       